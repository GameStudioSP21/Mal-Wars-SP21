local LOCAL_PLAYER = Game:GetLocalPlayer()

local FIRE_BIND = "primary_ability"

LOCAL_PLAYER.bindingPressedEvent:connect(function(_, binding)
        --check if the player is in the menu or build mode
        
    end
)